
# Webhook

import Resource from '@site/src/components/Resource';

<Resource json={{"allOf":[{"$ref":"#/components/schemas/WebhookBase"},{"type":"object","required":["secret"],"properties":{"secret":{"description":"The last 4 characters of the secret key used to generate the webhook payload HMAC.","type":"string"}}}],"component":"Webhook"}}/>
